/**
 * Setup file that runs after the test environment is set up
 * This runs for each test file
 */

// Timeout is configured in jest.config.js

// Suppress console logs during tests (optional - uncomment if needed)
// global.console = {
//     ...console,
//     log: jest.fn(),
//     debug: jest.fn(),
//     info: jest.fn(),
//     warn: jest.fn(),
// };
